<UserControl x:Class="VideohostingPanel.Elements.VideoItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VideohostingPanel.Elements"
             mc:Ignorable="d" MouseUp="GoChangeVideo">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Classes/Other/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Background="{StaticResource DarkTertiaryBrush}"
            CornerRadius="15"
            Margin="0,0,0,15"
            BorderBrush="{StaticResource PrimaryPurpleBrush}"
            BorderThickness="1"
            Cursor="Hand">

        <Border.Triggers>
            <EventTrigger RoutedEvent="Border.MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Duration="0:0:0.2"
                                       Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                       To="1.02"/>
                        <DoubleAnimation Duration="0:0:0.2"
                                       Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                       To="1.02"/>
                        <ColorAnimation Duration="0:0:0.2"
                                       Storyboard.TargetProperty="BorderBrush.Color"
                                       To="#FF0000"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="Border.MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Duration="0:0:0.2"
                                       Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                                       To="1"/>
                        <DoubleAnimation Duration="0:0:0.2"
                                       Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                                       To="1"/>
                        <ColorAnimation Duration="0:0:0.2"
                                       Storyboard.TargetProperty="BorderBrush.Color"
                                       To="#8A2BE2"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>

        <Border.RenderTransform>
            <ScaleTransform ScaleX="1" ScaleY="1"/>
        </Border.RenderTransform>

        <Grid Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Постер (иконка) -->
            <Border Grid.Column="0"
                    Width="60"
                    Height="60"
                    Background="{StaticResource DarkBackgroundBrush}"
                    CornerRadius="10"
                    Margin="0,0,15,0">
                <Path Data="M0,0 L40,0 L40,30 L0,30 Z M5,5 L35,5 L35,25 L5,25 Z"
                      Fill="{StaticResource PrimaryRedBrush}"
                      Width="30"
                      Height="30"
                      Stretch="Uniform"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
            </Border>

            <!-- Информация о видео -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="VideoNameText"
                          Text="Название видео"
                          FontSize="16"
                          FontWeight="Bold"
                          Foreground="{StaticResource TextPrimaryBrush}"/>

                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <TextBlock x:Name="AuthorText"
                              Text="Автор: Иван Иванов"
                              FontSize="12"
                              Foreground="{StaticResource TextSecondaryBrush}"/>
                </StackPanel>

                <!-- Статус верификации -->
                <Border x:Name="VerificationBadge"
                        Background="#33FF0000"
                        CornerRadius="10"
                        Padding="8,3"
                        Margin="0,5,0,0"
                        HorizontalAlignment="Left">
                    <TextBlock x:Name="VerificationText"
                              Text="Не верифицировано"
                              FontSize="10"
                              FontWeight="Bold"
                              Foreground="#FFFF8888"/>
                </Border>
            </StackPanel>

            <!-- Статистика (только текст, без иконок) и Дата -->
            <StackPanel Grid.Column="2" 
                       Orientation="Vertical"
                       VerticalAlignment="Center"
                       Margin="0,0,15,0">

                <!-- Дата в правом верхнем углу -->
                <TextBlock x:Name="DateText"
                          Text="01.01.2024"
                          FontSize="11"
                          Foreground="{StaticResource TextSecondaryBrush}"
                          HorizontalAlignment="Right"
                          Margin="0,0,0,10"/>

                <!-- Просмотры -->
                <TextBlock x:Name="ViewsCountText"
                          Text="Просмотров: 1234"
                          FontSize="11"
                          Foreground="{StaticResource TextSecondaryBrush}"
                          HorizontalAlignment="Right"
                          Margin="0,0,0,3"/>

                <!-- Лайки -->
                <TextBlock x:Name="LikesCountText"
                          Text="Лайков: 56"
                          FontSize="11"
                          Foreground="{StaticResource TextSecondaryBrush}"
                          HorizontalAlignment="Right"
                          Margin="0,0,0,3"/>

                <!-- Дизлайки -->
                <TextBlock x:Name="DislikesCountText"
                          Text="Дизлайков: 3"
                          FontSize="11"
                          Foreground="{StaticResource TextSecondaryBrush}"
                          HorizontalAlignment="Right"/>
            </StackPanel>

            <!-- ID видео -->
            <TextBlock x:Name="VideoIdText"
                       Grid.Row="0"
                       Grid.Column="3"
                       Text="#12345"
                       FontSize="11"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource TextSecondaryBrush}"/>
        </Grid>
    </Border>
</UserControl>